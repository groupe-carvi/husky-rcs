# Husky Robot Control Server Environment Configuration

# WebSocket server host (default: 0.0.0.0 - all interfaces)
# Use "localhost" or "127.0.0.1" for local connections only
HUSKY_WEBSOCKET_HOST=0.0.0.0

# WebSocket server port (default: 8767)
HUSKY_WEBSOCKET_PORT=8767

# ROS2 domain ID (optional, used by ROS2 environment)
# ROS_DOMAIN_ID=0